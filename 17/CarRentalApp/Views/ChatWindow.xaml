<Window x:Class="CarRentalApp.Views.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CarRentalApp"
        Title="💬 Чат"
        Height="500"
        Width="700"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <local:SenderToAlignmentConverter x:Key="SenderToAlignmentConverter"/>

        <Style TargetType="ListBox">
            <Setter Property="BorderThickness"
                    Value="1"/>
            <Setter Property="BorderBrush"
                    Value="LightGray"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Список пользователей онлайн -->
        <Border Grid.Column="0"
                BorderBrush="LightGray"
                BorderThickness="0,0,1,0"
                Background="#F5F5F5">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Text="👥 Онлайн пользователи"
                           FontWeight="Bold"
                           Padding="10"
                           Background="#E0E0E0"/>

                <ListBox ItemsSource="{Binding OnlineUsers}"
                         SelectedItem="{Binding SelectedUser}"
                         Background="Transparent"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal"
                                        Margin="5">
                                <Ellipse Width="10"
                                         Height="10"
                                         Fill="LightGreen"
                                         Margin="0,0,8,0"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding FullName}"
                                               FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding Role}"
                                               FontSize="10"
                                               Foreground="Gray"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <!-- Область чата -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок чата -->
            <Border Grid.Row="0"
                    Background="#2196F3"
                    Padding="10">
                <TextBlock Foreground="White"
                           FontWeight="Bold">
                    <Run Text="Чат с: "/>
                    <Run Text="{Binding SelectedUser.FullName, TargetNullValue='Выберите пользователя'}"/>
                </TextBlock>
            </Border>

            <!-- Сообщения -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          Padding="10">
                <ItemsControl ItemsSource="{Binding Messages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5"
                                    Padding="10"
                                    CornerRadius="8"
                                    MaxWidth="400"
                                    HorizontalAlignment="{Binding Sender, 
                                        Converter={StaticResource SenderToAlignmentConverter}}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Sender, 
                                                RelativeSource={RelativeSource AncestorType=Window}, 
                                                Path=DataContext.CurrentUserName}"
                                                         Value="True">
                                                <Setter Property="Background"
                                                        Value="#DCF8C6"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                        <Setter Property="Background"
                                                Value="#E3E3E3"/>
                                    </Style>
                                </Border.Style>

                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}"
                                               FontWeight="Bold"
                                               FontSize="11"
                                               Foreground="#555"/>
                                    <TextBlock Text="{Binding Message}"
                                               TextWrapping="Wrap"
                                               Margin="0,3"/>
                                    <TextBlock Text="{Binding FormattedTime}"
                                               FontSize="10"
                                               Foreground="Gray"
                                               HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Статус -->
            <TextBlock Grid.Row="2"
                       Text="{Binding StatusMessage}"
                       Padding="10,5"
                       Background="#FFF9C4"
                       FontSize="11"
                       Visibility="{Binding StatusMessage, 
                           Converter={StaticResource StringToVisibilityConverter}}"/>

            <!-- Поле ввода -->
            <Grid Grid.Row="3"
                  Background="#F5F5F5"
                  Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}"
                         Padding="10"
                         FontSize="14"
                         VerticalContentAlignment="Center">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter"
                                    Command="{Binding SendMessageCommand}"/>
                    </TextBox.InputBindings>
                </TextBox>

                <Button Grid.Column="1"
                        Content="📤 Отправить"
                        Command="{Binding SendMessageCommand}"
                        Padding="15,10"
                        Margin="5,0,0,0"
                        Background="#2196F3"
                        Foreground="White"
                        FontWeight="Bold"
                        Cursor="Hand"/>
            </Grid>
        </Grid>
    </Grid>
</Window>